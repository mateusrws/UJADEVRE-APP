export interface Address {
    add_id: string;
    add_bairro: string;
    add_cidade: string;
    add_uf: string;
    add_cep: string;
    add_rua: string;
    add_comp?: string | null;
    createdAt: string;
    updatedAt: string;
}
